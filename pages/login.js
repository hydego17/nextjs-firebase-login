import React, { useState } from "react"
import firebaseClient from "../firebaseClient"
import fireabase from "fire/app"
import "firebase/auth"

import {
  Box,
  Flex,
  Input,
  FormControl,
  FormLabel,
  FormHelperText,
  Stack,
  Button,
  Heading,
  useToast,
} from "@chakra-ui/core"

export default function Login() {
  firebaseClient()
}
